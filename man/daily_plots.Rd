% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daily_plots.R
\name{daily_plots}
\alias{daily_plots}
\title{Plot Daily Aggregated Time Series Data}
\usage{
daily_plots(df, x, y, fun = sum, interactive = FALSE, cadence_cutoff = 0)
}
\arguments{
\item{df}{A data frame containing the minute-level time series data.}

\item{x}{A string specifying the **timestamp** column name in `df` (must be POSIXct).}

\item{y}{A string specifying the **numeric value** column to aggregate and plot (e.g., 'steps').}

\item{fun}{A function used to aggregate the daily data (e.g., `sum`, `mean`, `median`). Default is `sum`.}

\item{interactive}{Logical; if TRUE, returns a `plotly` object and the aggregated data. If FALSE, returns a `ggplot2` object.}

\item{cadence_cutoff}{Numeric; The minimum required value in the `y` column for a minute to be included in the daily accumulation (e.g., set to 80 to count minutes with 80+ steps/min). Default is 0.}
}
\value{
If `interactive = FALSE`, returns a single `ggplot2` object.
        If `interactive = TRUE`, returns a **list** with two elements:
        \itemize{
          \item \code{plot}: The `plotly` object.
          \item \code{data}: A data frame containing the daily aggregated values used for the plot.
        }
}
\description{
This function aggregates a numeric variable (`y`) by day from a minute-level time series
(`df`) and creates a responsive bar chart visualization. Days with less than 2 hours
(120 minutes) of recorded data are automatically excluded from the analysis.
}
